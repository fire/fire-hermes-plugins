{
  "name": "recall_plans",
  "description": "Recall past planning contexts from holographic memory using HRR algebra. Single entity uses probe() (HRR unbind from cat:planning bank); multiple entities use reason() (compositional AND across entity probes).",
  "parameters": {
    "type": "object",
    "properties": {
      "entities": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Entities to recall plans for."
      },
      "domain": {
        "type": "string",
        "description": "Optional: filter by planning domain."
      },
      "limit": {
        "type": "integer",
        "description": "Maximum results to return. Default: 5."
      }
    },
    "required": ["entities"]
  }
}
